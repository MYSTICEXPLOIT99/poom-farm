local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Workspace = game:GetService("Workspace")
local PathfindingService = game:GetService("PathfindingService")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")
local HttpService = game:GetService("HttpService")
local Stats = game:GetService("Stats")
local Debris = game:GetService("Debris")
local GuiService = game:GetService("GuiService")
local VIM = game:GetService("VirtualInputManager")

local LocalPlayer = Players.LocalPlayer
local player = LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local Net = require(ReplicatedStorage.Modules.Core.Net)
local CharModule = require(ReplicatedStorage.Modules.Core.Char)
local RagdollModule = require(ReplicatedStorage.Modules.Game.Ragdoll)

local Done = false
local LogoID = "rbxassetid://116316450260880"
_G.IsGodMode = false

local function checkCondition()
    local splash = playerGui:FindFirstChild("SplashScreenGui")
    if not splash then
        return false
    end

    local frame = splash:FindFirstChild("Frame")
    local playButton = frame and frame:FindFirstChild("PlayButton")

    return playButton and playButton.Visible == true
end

local canShowUI = checkCondition()

local function pressEnter(guiObject)
    if not guiObject then
        return
    end

    GuiService.SelectedObject = guiObject

    VIM:SendKeyEvent(true, Enum.KeyCode.Return, false, game)
    task.wait(0.05)
    VIM:SendKeyEvent(false, Enum.KeyCode.Return, false, game)

    task.wait(0.2)

    GuiService.SelectedObject = nil
end

if canShowUI then
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "Vexinity Exploit"
    screenGui.ResetOnSpawn = false
    screenGui.IgnoreGuiInset = true
    screenGui.Parent = playerGui

    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.fromScale(0.35, 0)
    mainFrame.Position = UDim2.fromScale(0.5, 0.5)
    mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
    mainFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    mainFrame.BackgroundTransparency = 0.4
    mainFrame.BorderSizePixel = 0
    mainFrame.AutomaticSize = Enum.AutomaticSize.Y
    mainFrame.Parent = screenGui

    Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 15)

    local frameStroke = Instance.new("UIStroke")
    frameStroke.Color = Color3.fromRGB(255, 255, 255)
    frameStroke.Transparency = 0.8
    frameStroke.Thickness = 1
    frameStroke.Parent = mainFrame

    local padding = Instance.new("UIPadding")
    padding.PaddingTop = UDim.new(0, 20)
    padding.PaddingBottom = UDim.new(0, 20)
    padding.Parent = mainFrame

    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 12)
    layout.HorizontalAlignment = Enum.HorizontalAlignment.Center
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Parent = mainFrame

    local logo = Instance.new("ImageLabel")
    logo.Size = UDim2.fromOffset(80, 80)
    logo.BackgroundTransparency = 1
    logo.Image = LogoID
    logo.LayoutOrder = 0
    logo.Parent = mainFrame

    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 35)
    title.BackgroundTransparency = 1
    title.Text = "Vexinity Exploit"
    title.TextColor3 = Color3.fromRGB(255, 255, 255)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 28
    title.LayoutOrder = 1
    title.AutoLocalize = false
    title.Parent = mainFrame

    local subtitle = Instance.new("TextLabel")
    subtitle.Size = UDim2.new(0.9, 0, 0, 20)
    subtitle.BackgroundTransparency = 1
    subtitle.Text = "Mode you want to play"
    subtitle.TextColor3 = Color3.fromRGB(200, 200, 200)
    subtitle.Font = Enum.Font.Gotham
    subtitle.TextSize = 14
    subtitle.LayoutOrder = 2
    subtitle.AutoLocalize = false
    subtitle.Parent = mainFrame

    local function createButton(text, order)
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0.85, 0, 0, 45)
        btn.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        btn.BackgroundTransparency = 0.9
        btn.Text = text
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        btn.Font = Enum.Font.GothamMedium
        btn.TextSize = 16
        btn.LayoutOrder = order
        btn.AutoLocalize = false
        btn.Parent = mainFrame

        Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)

        local btnStroke = Instance.new("UIStroke")
        btnStroke.Color = Color3.fromRGB(255, 255, 255)
        btnStroke.Transparency = 0.7
        btnStroke.Parent = btn

        return btn
    end

    local function destroyUI()
        local tween = TweenService:Create(mainFrame, TweenInfo.new(0.3), {
            BackgroundTransparency = 1,
            Size = UDim2.new(0, 0, 0, 0)
        })

        tween:Play()
        tween.Completed:Wait()

        screenGui:Destroy()
    end

    local normalBtn = createButton("Normol Mode", 3)

    local line = Instance.new("Frame")
    line.Size = UDim2.new(0.75, 0, 0, 1)
    line.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    line.BackgroundTransparency = 0.8
    line.BorderSizePixel = 0
    line.LayoutOrder = 4
    line.Parent = mainFrame

    local godBtn = createButton("God Mode", 5)

    normalBtn.MouseButton1Click:Connect(function()
        _G.IsGodMode = false

        task.spawn(function()
            local splash = playerGui:FindFirstChild("SplashScreenGui")
            local frame = splash and splash:FindFirstChild("Frame")

            if splash and splash.Enabled then
                pressEnter(frame and frame:FindFirstChild("PlayButton"))
            end

            task.wait(1.5)

            local creator = playerGui:FindFirstChild("CharacterCreator")

            if creator then
                local menu = creator:FindFirstChild("MenuFrame")
                local skipBtn = menu and menu:FindFirstChild("AvatarMenuSkipButton")

                pressEnter(skipBtn)
            end

            task.wait(0.5)

            destroyUI()
            Done = true
        end)
    end)

    godBtn.MouseButton1Click:Connect(function()
        _G.IsGodMode = true

        task.spawn(function()
            destroyUI()

            if not _G.Bypass then
                local func = getupvalue(Net.get, 2)

                if func then
                    setconstant(func, 3, "KUYIENGOKUYIENGO")
                    setconstant(func, 4, "KUYIENGOKUYIENGO")
                end

                _G.Bypass = true
            end

            local old

            old = hookfunction(Net.send, function(...)
                local args = {...}

                if args[1] == "leave_character_creator"
                or args[1] == "player_created_outfit" then
                    return nil
                end

                return old(...)
            end)

            task.wait(2.5)

            local splash = playerGui:FindFirstChild("SplashScreenGui")
            local frame = splash and splash:FindFirstChild("Frame")

            if splash and splash.Enabled then
                pressEnter(frame and frame:FindFirstChild("PlayButton"))
            end

            task.wait(4)

            local creator = playerGui:FindFirstChild("CharacterCreator")

            if creator then
                local menu = creator:FindFirstChild("MenuFrame")
                local skipBtn = menu and menu:FindFirstChild("AvatarMenuSkipButton")

                pressEnter(skipBtn)
            end

            task.wait(2.5)

            replicatesignal(game.Players.LocalPlayer.Kill)

            task.wait(7)

            Net.send("death_screen_request_respawn")

            Done = true
        end)
    end)
else
    Done = true
end

repeat
    task.wait()
until Done

if not _G.IsGodMode then
    local send = Net.send
    local kickF = getupvalue(send, 2)

    local function
